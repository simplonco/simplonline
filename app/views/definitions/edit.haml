
%h3= t('.title')

= form_for @definition do |l|

  = render partial: 'form', locals: {l: l}

  %p
    = l.submit t('.update'), class: 'button'
    = link_to t('.cancel'), definitions_path, class: 'button'
