%h2= t('.title')

= form_for(Exercice.new) do |exo|

  = render partial: "form", locals: {exo:exo}

  %p
    = exo.submit t('.submit'), class: 'button'
    = link_to t('.cancel'), lesson_path(@lesson)
