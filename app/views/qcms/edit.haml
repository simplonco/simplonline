%h2= t('.title')

= form_for @qcm, url: lesson_qcm_path(@qcm, @lesson) do |f|

  = render partial: 'form', locals: {f: f}

  %p
    = f.submit t('.update')
    = link_to t('.cancel'), lesson_qcm_path(@lesson, @qcm)

