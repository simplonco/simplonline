
%h2
  = t('.title', name: @user.name)

%ul.chapter-list
  - @user.chapters.each do |chapter|
    %li= link_to chapter.title, lesson_chapter_path(chapter.lesson, chapter)

%p
  - if @user == current_user
    = link_to t('.edit'), edit_user_path(current_user)
