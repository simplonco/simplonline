%h2
  = t('users.show.title', name: @user.name)

%section.last-messages
  %h1= t('users.show.last-message-title')
  %ul
    %li
      message de Raymond, le 22/9/2014 à 22h34
      %em
        non mais c'est quoi ce binzzz

%section.last-lessons
  %h1= t('users.show.last-lessons-title')
  %ul
    - @last_lessons.each do |lesson|
      %li
        = link_to lesson.title, lesson_path(lesson)
  %p
    = link_to t('lessons.index.create_lesson'), lessons_path

  - if current_user.local?
    %p
      = link_to t('lessons.index.create_lesson'), new_lesson_path
