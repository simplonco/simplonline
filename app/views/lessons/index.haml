
%h3
  = t('.title')

%ul
  - @lessons.each do |lesson|
    %li
      = link_to lesson.title, edit_lesson_path(lesson)

- if current_user && current_user.local?
  = link_to t('.create_lesson'), new_lesson_path
