%h3= t('.title')

= form_for @lesson do |l|

  = render partial: 'form', locals: {l:l}

  %p
    = l.submit t('.update'), class: 'button'
    = link_to t('.cancel'), lesson_path(@lesson), class: 'button'

