%p
  = l.label :title, t('.title_field')
  %br
  = l.text_field :title, placeholder: t('placeholder.title'), required: true

%p
  = l.label :publish_at, t('.publish_at_field')
  %br
  = l.date_field :publish_at, placeholder: t('placeholder.date'), required: true

%p
  = l.label :description, t('.description')
  %br
  = l.text_area :description, placeholder: t('placeholder.description')
